/* SG_CUBE Terminal UI — neon blue on black */

Screen {
    background: #000000;
    color: #00d8ff;
}

/* ── Wordmark ────────────────────────────────────────── */

#wordmark-box {
    height: 7;
    align: center middle;
    padding: 0;
    background: #000000;
}

#wordmark {
    color: #00d8ff;
    text-style: bold;
    content-align: center middle;
    width: auto;
    height: 7;
}

/* ── Status bar ──────────────────────────────────────── */

#status-bar {
    layout: horizontal;
    height: 3;
    border: solid #00d8ff;
    padding: 0 2;
    margin: 1 2 0 2;
    background: #000000;
}

#status-left {
    width: 1fr;
    color: #00d8ff;
    content-align: left middle;
    height: 1fr;
}

#status-clock {
    width: auto;
    color: #ffffff;
    text-style: bold;
    content-align: center middle;
    height: 1fr;
}

#status-right {
    width: 1fr;
    color: #00d8ff;
    content-align: right middle;
    height: 1fr;
}

/* ── Main two-column row ─────────────────────────────── */

#main-row {
    height: 1fr;
    margin: 0 2;
}

#left-col, #right-col {
    width: 1fr;
    height: 100%;
}

Panel {
    border: round #005577;
    height: 1fr;
    min-height: 4;
    margin: 1 0 0 0;
    padding: 0 1;
    background: #000000;
}

Panel > .panel-body {
    color: #00d8ff;
    padding: 0 1;
    height: 1fr;
}

Panel:focus-within {
    border: round #00d8ff;
}

/* ── Live mic strip ──────────────────────────────────── */

#mic-strip {
    layout: horizontal;
    height: 3;
    border: solid #00d8ff;
    padding: 0 2;
    margin: 1 2 0 2;
    background: #000000;
}

#mic-label {
    width: auto;
    color: #005577;
    text-style: bold;
    content-align: left middle;
    height: 1fr;
    padding: 0 2 0 0;
}

#mic-bars {
    width: 1fr;
    color: #ffffff;
    content-align: left middle;
    height: 1fr;
}

/* ── Footer ──────────────────────────────────────────── */

Footer {
    background: #000811;
    color: #00aadd;
}

Footer > .footer--description {
    color: #00d8ff;
}

Footer > .footer--key {
    background: #00152a;
    color: #ffffff;
}
